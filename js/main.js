document.addEventListener('DOMContentLoaded',()=>{const ano=document.getElementById('ano');if(ano)ano.textContent=new Date().getFullYear();
const doar=document.getElementById('doar');const status=document.getElementById('status-doacao');if(doar&&status){doar.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(doar);status.textContent=`Obrigado! Simulamos sua doação de R$ ${f.get('valor')} para "${f.get('campanha')}".`;});}
const form=document.getElementById('form-cadastro');const st=document.getElementById('status');if(form){form.addEventListener('submit',e=>{if(!form.checkValidity()){e.preventDefault();form.reportValidity();st.textContent='Por favor, corrija os campos destacados.';}else{e.preventDefault();st.textContent='Cadastro enviado com sucesso (simulação).';form.reset();}});}});